# Fixed two-stage Poisson diagnostic refitter

## Decision and boundary

The user requested “按计划推进” after the proposed fixed localization → TRF direction. Proceed with this numerical subplan; do not repeat the precision-v2 closeout or require another direction selection. The earlier precision plan remains completed historical work. ACF/RMS, new independent statistical registration, release and old-format compatibility are outside this change.

Evidence root: `/Users/dala/Desktop/XRR-Fitter/.codex-artifacts/poisson-diagnostic-20260913/two-stage-001`. `baseline-source/` is the current precision-v2 tree, not the recovered v1 experiment. Original failures and probes remain immutable.

The root cause is the low-count Gauss–Newton curvature overestimate (about 9.5/22.7 times the complete curvature on the two exposed bad paths). A scalar-only replacement was rejected because near-minimum line search can terminate ABNORMAL. This design uses complete-score localization to reach that neighborhood, then the already approved TRF step/gradient criteria. It is a structural candidate, not a claim of optimality or scientific acceptance.

## Per-start numerical contract

1. Preserve the declaration plus three fixed, deduplicated Sobol starts and current joint compilation. Do not accept the product winner as a diagnostic warm start.
2. For p > 0, let `M = max(local_min_nfev, local_nfev_per_parameter * max(1, p))`. Reserve one full handoff validation and at least half the remaining requests for refinement: `L = (M - 1) // 2`. M < 3 fails explicitly without increasing the budget. A zero-dimensional model retains one publication evaluation and zero optimization work.
3. Every nonzero-dimensional path runs L-BFGS-B localization (`jac=True`, original unit bounds, `ftol=gtol=1e-10`, maxfun/maxiter L) then TRF, never only after failure. The callback hard cap, not SciPy's advisory maxfun, enforces L. Localization evaluates the complete current loss `Q = .5 * sum(rho(r*r)[0])`, `g = J.T @ (rho(r*r)[1] * r)`, including priors and joint weighting.
4. The handoff coordinate is always the last accepted iterate, copied at the accepted-iteration callback. Its initial state is the declared point only after a real finite initial Q/g request. A normal return, ABNORMAL return, or hard localization cap does not change this rule. Never choose among returned x, last trial, and initial x after examining their scores. Never use `OptimizeResult.fun` as Q at returned x.
5. Re-evaluate that fixed coordinate on the complete original model/reporting axes, charging exactly one request. Invalid model, axes, noise, objective or fitted values fails the path, with no replacement candidate. A thrown numerical failure, nonfinite residual/J/Q/g or cancellation aborts immediately; a valid earlier point does not hide it. Unexpected programming errors propagate unchanged. Existing finite physical-constraint trial sentinels remain the underlying system contract; the handoff cannot publish them as a valid model.
6. TRF receives exactly `M - spent` remaining requests, original analytic residual/J and loss, `bounds=(0,1)`, `x_scale='jac'`, Poisson `ftol=None`, `xtol=gtol=1e-10`. Final solver success remains necessary; preserve the true exit status/message rather than claiming gradient convergence for an xtol exit. Keep the final full publication evaluation and all original validity checks. That publication traversal remains excluded from optimizer work, as before.
7. Count actual Q/g or residual callback requests, plus handoff, not Jacobian cache requests. A Jacobian request must match the latest charged residual coordinate; an unmatched request is a programming error before any new system evaluation. Validate both residual and Jacobian at the complete system/cache entry, including rejected TRF trials. Do not replace counted work with unverified OptimizeResult.nfev. A callback guard prevents any request beyond the current phase/total cap. All paths share the same estimator, but each retains its own original budget. One final path failure invalidates the whole refit; no successful subset or replacement start.
8. Poll cancellation before and after evaluations, accepted-iterate callbacks, stage transitions and final solver returns. Preserve SearchCancelled and existing service boundaries. Arrays exposed to either optimizer and handoff are copied.

## Evidence and current identity

The numerical kernel records each attempted stage's phase, raw SciPy status (nullable for non-SciPy exits), success flag, original message and actual nfev. Explicit cap/validation/numerical exits have descriptive non-SciPy messages, never fabricated SciPy statuses.

Runtime path records bind that work to M. `DiagnosticSolverExit` distinguishes native, cap, validation, numerical failure, not-entered and zero-dimensional states without inventing native statuses. Every `DiagnosticPathSummary` contains M, the ordered three exits and a repetition count. `DiagnosticRefitWork` is a canonical joint histogram of these complete path summaries, bound to the deduplicated declared-path count. This compact joint representation improves on separate phase marginals: readers can independently recompute every stage cap, path total, overall nfev and maximum, without a separately asserted max or full trajectories. Calibration stores separate `observed_work` and `null_work`. Existing indexed failure_reasons still identify failed observed/null refits; histogram bins are not claimed to preserve per-replicate/starting-point ordering. Validators enforce count types, canonical unique bins, phase transitions, exact cost and available-family path counts; content sealing covers every field. No missing-field fallback or legacy parser. Escaped callback numerical exceptions propagate rather than fabricating zero work; the refitter owns expected numerical failure accounting.

Use one strict estimator identity: algorithm `xrr-fit-v2-poisson-3`, diagnostic `poisson-refit-null-v3`, method `poisson_refit_null_v3`, policy `declared_sobol4_lbfgsb_trf_v3`. Objective remains string `"2"`; the likelihood is unchanged. Schema 4 is still unreleased and its current evidence shape is extended strictly. Older estimator identities and missing evidence fields are rejected. The old acceptance protocol's v1 registration/review hash stays untouched and must continue rejecting this runtime.

## Verification and scientific boundary

First obtain real RED on original 119/null322 and 148/null493, generated with the original product-source coordinates and service diagnostic seed. Include newly exposed 119/null7 and footprint/surface37/null0 as development controls. Then test caps, rejected-trial/returned-fun mismatch, accepted-array mutation, invalid handoff, all error/cancellation boundaries, priors, joint/global dimensions, bounds, zero dimensions and genuine nonconvergence. Non-Poisson product solvers and ACF remain unchanged.

After focused GREEN, run all four existing full B999 development API replays, strict save/load and phase/budget accounting. These exposed seeds are development evidence only, never a new coverage estimate. Run relevant official verification MODEs and full Radon on a bound final snapshot, then independent scoped review. New unexposed-seed scientific acceptance remains separate; do not erase the old 4/8 endpoint FAIL.
